// Rajpatra AI — placeholder test
// Full tests require a running Supabase instance and Groq API key.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
